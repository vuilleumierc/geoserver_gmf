[geoserver_gmf]
host=${PGHOST_SLAVE}
port=${PGPORT_SLAVE}
#sslmode=${PGSSLMODE}
user=${PGUSER}
password=${PGPASSWORD}
dbname=${PGDATABASE}

[geoserver_gmf_master]
host=${PGHOST}
port=${PGPORT}
#sslmode=${PGSSLMODE}
user=${PGUSER}
password=${PGPASSWORD}
dbname=${PGDATABASE}
